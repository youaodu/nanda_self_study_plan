单项选择

每当一个“中断”事件发生后，会暂停原来程序的执行，把CPU的控制转移给
A、中断服务程序     B、系统调用程序
C、系统引导程序     D、优先级最高的用户程序

自愿性中断是
A、硬件故障中断     B、访管中断
C、程序性中断       C、外部中断

计算机系统中判别是否有中断事件发生应是在（）B
A、进程切换时       B、执行完一条指令后
B、执行P操作后      C、由用户态转入核心态时。


多项选择题

属于“强迫性中断事件”的是
A、主存读写发生校验错       B、程序性中断
C、访管中断               D、输入/输出中断
E、外部中断



简答题

简述中断装置的主要职能。
    答案：
    1.检查是否有中断事件发生
    2.若有中断发生，保护好被中断进程的断点及现场信息。
    3.启动操作系统的中断处理程序。